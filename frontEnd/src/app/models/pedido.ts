export interface Pedidos {
    createdAt?: string,
    cantidad: number,
    total: number,
    fecha_p: Date,
    fecha_e: Date,
    dispositivo: string,
    user: string,
    updatedAt?: string,
    _id: string
    }